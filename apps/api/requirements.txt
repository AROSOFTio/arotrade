# FastAPI & Web
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
cryptography==41.0.7
bcrypt==4.1.1

# Task Queue
celery[redis]==5.3.4

# HTTP & API
httpx==0.25.2
requests==2.31.0

# AI Providers
# Pin protobuf directly: google-ai-generativelanguage (pulled in by
# google-generativeai) requires protobuf<6.0.0, which conflicts with newer
# grpcio-status releases that require protobuf>=7. Fixing protobuf up front
# removes that axis from resolution so pip doesn't backtrack through dozens
# of grpcio-status candidates.
protobuf==5.29.5
google-generativeai==0.8.5
# openai==1.3.10  # Future
# groq==0.4.1  # Future
# anthropic==0.7.10  # Future

# Trading integrations
# MetaApi for MT4/MT5 connectivity — the ONLY broker integration for MT5 accounts
# metaapi-cloud-sdk>=27.0.0  # Full async SDK — uncomment for streaming
# Using httpx REST calls (metaapi_gateway.py) for simplicity in Celery context
websocket-client==1.6.4  # Retained for any direct WS needs

# Utilities
python-dotenv==1.0.0
pytz==2023.3
jinja2==3.1.2
email-validator==2.1.1

# Logging & Monitoring
python-json-logger==2.0.7
sentry-sdk==1.38.0

# CORS
fastapi-cors==0.0.6

# Rate Limiting
slowapi==0.1.9

# Utilities
numpy==1.26.2
pandas==2.1.3
